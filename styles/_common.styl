$colours = {
    cta: #7B6CED
    bodyBg: #2a2a35
    codeBg: rgba(#000, 0.07)
    subhead: #d4d2ef
    text: white
}

$breakpoints = {
    midsize: 500px,
    desktop: 800px
}

$dimensions = {
    leftColWidth: 20%,
    gutterWidth: 1rem
}

$mainFont = Roboto, system-ui, sans-serif
