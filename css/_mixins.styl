// Helpful utilities stolen from the stylus docs:
vendor(prop, args)
    -webkit-{prop} args
    -moz-{prop} args
    {prop} args

clearfix()
    &:before
    &:after
        content: ""
        display: table
    &:after
        clear: both
    zoom: 1

